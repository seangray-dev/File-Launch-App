export type RecentFilesProps = {
  setCurrentView: React.Dispatch<React.SetStateAction<string>>;
};

export type NoBaseFolderProps = {
  setCurrentView: React.Dispatch<React.SetStateAction<string>>;
};
